# Examples

## minimal-api-web

Generated by:

```bash
ruby scripts/init_product.rb \
  --path examples/minimal-api-web \
  --name minimal-api-web \
  --display-name "Minimal API + Web" \
  --preset api-web \
  --mode link \
  --force
```

Then:

```bash
cd examples/minimal-api-web
ruby scripts/doctor.rb
ruby scripts/validate_workflow.rb
# optional: npm install / npm test under backend after stubs are ready
```

Re-generate with `--force` when kit templates change.
