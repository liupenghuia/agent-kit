checks:
  - id: backend-test
    when: "scopes.backend"
    cwd: backend
    cmd: ["npm", "test"]
  - id: backend-syntax-app
    when: "scopes.backend"
    cwd: backend
    cmd: ["node", "--check", "src/app.js"]
